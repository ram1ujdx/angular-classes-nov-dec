import { AppGreenifyDirective } from './app-greenify.directive';

describe('AppGreenifyDirective', () => {
  it('should create an instance', () => {
    const directive = new AppGreenifyDirective();
    expect(directive).toBeTruthy();
  });
});
